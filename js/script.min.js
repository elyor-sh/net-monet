"use strict";

function _toConsumableArray(arr) { return _arrayWithoutHoles(arr) || _iterableToArray(arr) || _unsupportedIterableToArray(arr) || _nonIterableSpread(); }

function _nonIterableSpread() { throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method."); }

function _unsupportedIterableToArray(o, minLen) { if (!o) return; if (typeof o === "string") return _arrayLikeToArray(o, minLen); var n = Object.prototype.toString.call(o).slice(8, -1); if (n === "Object" && o.constructor) n = o.constructor.name; if (n === "Map" || n === "Set") return Array.from(o); if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o, minLen); }

function _iterableToArray(iter) { if (typeof Symbol !== "undefined" && iter[Symbol.iterator] != null || iter["@@iterator"] != null) return Array.from(iter); }

function _arrayWithoutHoles(arr) { if (Array.isArray(arr)) return _arrayLikeToArray(arr); }

function _arrayLikeToArray(arr, len) { if (len == null || len > arr.length) len = arr.length; for (var i = 0, arr2 = new Array(len); i < len; i++) { arr2[i] = arr[i]; } return arr2; }

function testWebP(e) {
  var s = new Image();
  s.onload = s.onerror = function () {
    e(2 == s.height);
  }, s.src = "data:image/webp;base64,UklGRjoAAABXRUJQVlA4IC4AAACyAgCdASoCAAIALmk0mk0iIiIiIgBoSygABc6WWgAA/veff/0PP8bA//LwYAAA";
}

testWebP(function (e) {
  1 == e ? document.querySelector("body").classList.add("webp") : document.querySelector("body").classList.add("no-webp");
});
var burger = document.querySelector(".header__menu-burger"),
    menuBody = document.querySelector(".header__menu-body");
burger && burger.addEventListener("click", function () {
  burger.classList.toggle("_pressed"), burger.classList.contains("_pressed") ? (menuBody.classList.add("_active"), document.body.classList.add("_lock")) : (menuBody.classList.remove("_active"), document.body.classList.remove("_lock"));
});
var simpleSlider = new Swiper(".simple__slider", {
  pagination: {
    el: ".swiper-pagination",
    checkable: !0
  },
  spaceBetween: 15,
  simulateTouch: !0,
  slidesPerView: 3,
  breakpoints: {
    320: {
      slidesPerView: 1.3
    },
    480: {
      slidesPerView: 1.8
    },
    620: {
      slidesPerView: 2.4
    },
    768: {
      slidesPerView: 3,
      simulateTouch: !1
    }
  }
}),
    adaptationSlider = new Swiper(".adaptation__slider", {
  pagination: {
    el: ".adaptation-pagination",
    checkable: !0
  },
  spaceBetween: 15,
  simulateTouch: !0,
  slidesPerView: 3,
  breakpoints: {
    320: {
      slidesPerView: 1.3
    },
    480: {
      slidesPerView: 1.8
    },
    620: {
      slidesPerView: 2.4
    },
    768: {
      slidesPerView: 3,
      simulateTouch: !1
    }
  }
});

_toConsumableArray(document.querySelectorAll(".question__plus")).forEach(function (e) {
  e.addEventListener("click", function () {
    this.classList.toggle("_pressed");
    var e = this.closest(".question__box");
    this.classList.contains("_pressed") ? (e.classList.add("_active"), e.querySelector(".question__dropdown-text").classList.add("_more"), this.innerHTML = "-") : (e.classList.remove("_active"), e.querySelector(".question__dropdown-text").classList.remove("_more"), this.innerHTML = "+");
  });
});